name: 'Run Tests'
description: 'Run tests for halos-browser-trust (shellcheck only)'

runs:
  using: 'composite'
  steps:
    - name: Run shellcheck
      run: shellcheck usr/libexec/halos/*.sh
      shell: bash
